String generateN64GamesSQL() {
    def builder = new StringBuilder()
    def games = 
    builder.append("INSERT INTO \"game\" (name, platform_id) VALUES\n    (\n")

    builder.append(");\n")
    return builder.toString()
}

ext {
    generateN64GamesSQL = this.&generateN64GamesSQL
}